info:
  name: Login Error
  type: http
  seq: 2

http:
  method: POST
  url: "{{BaseUrl}}/auth/login"
  body:
    type: json
    data: |-
      {
        "username": "none",
        "password": "fulfill"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
