info:
  name: Buy off-menu
  type: http
  seq: 6

http:
  method: POST
  url: "{{BaseUrl}}/orders"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
