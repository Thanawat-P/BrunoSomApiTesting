info:
  name: Non-exist Order
  type: http
  seq: 4

http:
  method: GET
  url: "{{BaseUrl}}}/orders/ORD-NOT-REAL"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
