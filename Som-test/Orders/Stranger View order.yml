info:
  name: Stranger View order
  type: http
  seq: 7

http:
  method: GET
  url: "{{BaseUrl}}/orders/{{current_order_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
