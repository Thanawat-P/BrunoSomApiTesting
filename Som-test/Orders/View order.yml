info:
  name: View order
  type: http
  seq: 2

http:
  method: GET
  url: "{{BaseUrl}}/orders/{{order_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
